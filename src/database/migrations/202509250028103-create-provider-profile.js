"use strict";

module.exports = {
  async up(queryInterface, Sequelize) {
    await queryInterface.createTable("ProviderProfile", {
      id: {
        type: Sequelize.UUID,
        defaultValue: Sequelize.UUIDV4,
        primaryKey: true,
      },
      user_id: {
        type: Sequelize.UUID,
        allowNull: false,
        unique: true,
        references: { model: "Users", key: "id" },
        onDelete: "CASCADE",
      },
      display_name: { type: Sequelize.STRING(255), allowNull: true },
      bio: { type: Sequelize.TEXT, allowNull: true },
      service_radius_km: { type: Sequelize.DECIMAL(5, 2), allowNull: true },
      status: {
        type: Sequelize.ENUM("ONLINE", "OFFLINE", "BUSY"),
        allowNull: false,
        defaultValue: "OFFLINE",
      },
      avg_rating: {
        type: Sequelize.DECIMAL(3, 2),
        allowNull: false,
        defaultValue: 0,
      },
      rating_count: {
        type: Sequelize.INTEGER,
        allowNull: false,
        defaultValue: 0,
      },
      created_at: {
        type: Sequelize.DATE,
        allowNull: false,
      },
      updated_at: {
        type: Sequelize.DATE,
        allowNull: false,
      },
    });
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.dropTable("ProviderProfile");
    // Drop enum type generated by Sequelize for status
    await queryInterface.sequelize.query(
      'DROP TYPE IF EXISTS "Pnum_providerProfileStatus";'
    );
  },
};
